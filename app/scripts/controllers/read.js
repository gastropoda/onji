'use strict';

/**
 * @ngdoc function
 * @name onjiApp.controller:ReadCtrl
 * @description
 * # ReadCtrl
 * Controller of the onjiApp
 */
angular.module('onjiApp')
  .controller('ReadCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
