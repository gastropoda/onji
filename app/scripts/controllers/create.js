'use strict';

/**
 * @ngdoc function
 * @name onjiApp.controller:CreateCtrl
 * @description
 * # CreateCtrl
 * Controller of the onjiApp
 */
angular.module('onjiApp')
  .controller('CreateCtrl', function ($scope) {
  });
